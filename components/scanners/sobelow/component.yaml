name: sobelow
description: "Runs ncc sobelow for elixir and parses findings into OCSF format"
type: scanner
steps:
  - name: scanner
    image: components/scanners/sobelow/scanner
    env_vars:
      TERM: "dumb"
      ERL_FLAGS: "-noinput -noshell"
    executable: /bin/bash
    args:
      - -c
      - /entrypoint.sh {{ sourceCodeWorkspace }} {{ scratchWorkspace }}/results
  - name: parser
    image: components/scanners/sobelow
    env_vars:
      SOBELOW_RESULTS_DIR: "{{ scratchWorkspace }}/results"
      WORKSPACE_PATH: "{{ sourceCodeWorkspace }}"
    executable: /bin/app
